-- File uci/obserwrt.json --
{
  "obserwrt": {
    ".name": "main",
    "prometheus_textfile": "/tmp/obserwrt-prom-interval.prom",
    "prometheus_interval": "abc"
  }
}
-- End --

-- Testcase --
import * as metrics from 'metrics';

let died = false;

try {
	metrics.init();
}
catch (e) {
	died = true;
}

printf("died=%d\n", died);
-- End --

-- Expect stdout --
died=1
-- End --